INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

SET(SUBFOLDERS
    "${CMAKE_CURRENT_SOURCE_DIR}/data"
    )

FOREACH(ITEM ${SUBFOLDERS})
    MESSAGE("SUBFOLDER: ${ITEM}")
    INCLUDE_DIRECTORIES(${ITEM})
    ADD_SUBDIRECTORY(${ITEM})
ENDFOREACH()
